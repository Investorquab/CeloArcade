# Get a free WalletConnect/Reown project ID at https://cloud.reown.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Only needed once the faucet-claim API route is wired to sign real transactions.
# NEVER prefix this with NEXT_PUBLIC_ — it must stay server-side only.
OPERATOR_PRIVATE_KEY=
CELO_RPC_URL=https://forno.celo.org
